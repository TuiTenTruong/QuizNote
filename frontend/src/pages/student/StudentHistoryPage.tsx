import { ReactElement } from "react";
import { StudentHistorySection } from "../../sections/student/StudentHistorySection";

export const StudentHistoryPage = (): ReactElement => {
    return <StudentHistorySection />;
};