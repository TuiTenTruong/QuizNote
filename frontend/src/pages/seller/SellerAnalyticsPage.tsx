import { ReactElement } from "react";
import SellerAnalyticsSection from "../../sections/seller/SellerAnalyticsSection";

const SellerAnalyticsPage = (): ReactElement => {
    return <SellerAnalyticsSection />;
};

export default SellerAnalyticsPage;
