import { ReactElement } from "react";
import { StudentAnalyticSection } from "../../sections/student/StudentAnalyticSection";

export const StudentAnalyticPage = (): ReactElement => {
    return <StudentAnalyticSection />;
}